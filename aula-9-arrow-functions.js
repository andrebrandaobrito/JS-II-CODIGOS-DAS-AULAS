const soma = (a, b) => {
  return a + b;
};

const subtrai = (a, b) => {
  return a - b;
};

const divide = function (a, b) {
  return a / b;
};

const multiplica = (a, b) => a + b;

const res = multiplica(1, 2);
console.log(res);
